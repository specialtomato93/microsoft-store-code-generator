# microsoft-store-code-generator
A simple generator for codes in the Microsoft Store format. No validity guaranteed, but give it a whirl!
This is a python program that generates 25 character codes with dashes that are in the Microsoft Store code format.
There's also a webpage for using it with a simple UI
Not really much to say, so enjoy and feel free to use wherever.
